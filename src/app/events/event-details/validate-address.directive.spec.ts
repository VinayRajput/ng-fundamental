import { ValidateAddressDirective } from './validate-address.directive';

describe('ValidateAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateAddressDirective();
    expect(directive).toBeTruthy();
  });
});
